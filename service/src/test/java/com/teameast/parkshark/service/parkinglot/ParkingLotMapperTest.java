package com.teameast.parkshark.service.parkinglot;

import com.teameast.parkshark.domain.parkinglots.Category;
import com.teameast.parkshark.domain.parkinglots.ParkingLot;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class ParkingLotMapperTest {
  /*@Test
    void parkingLotDto() {
        //GIVEN
        Category category = new Category("ourCategory");
        ParkingLotMapper parkingLotMapper = new ParkingLotMapper(new CategoryMapper());
        ParkingLot parkingLot = ParkingLot.ParkingLotBuilder.parkingLotBuilder().withName("ourParkingLot").withMaxCapacity(100).withCategory(category).build();
        //WHEN
        ParkingLotDto parkingLotDto = parkingLotMapper.parkingLotToDto(parkingLot);
        //THEN
        Assertions.assertThat(parkingLotDto).isNotNull();
        Assertions.assertThat(parkingLotDto.getCategory().getCategory()).isEqualTo("ourCategory");
    }

    @Test
    void DtoToParkingLot() {
        //GIVEN
        CategoryDto categoryDto = new CategoryDto("ourCategoryDto");
        ParkingLotMapper parkingLotMapper = new ParkingLotMapper(new CategoryMapper());
        //ParkingLotDto parkingLotDto = new ParkingLotDto(1, "ourParkingLotDto", 100, category);
        //WHEN
        //ParkingLot parkingLot = parkingLotMapper.parkingLotDtoToParkingLot(parkingLotDto);
        //THEN
        Assertions.assertThat(parkingLot).isNotNull();
        Assertions.assertThat(parkingLot.getCategory().getCategory()).isEqualTo("ourCategoryDto");

    }*/
}